/*
 describe('MC Show Process Component Controller', function(){
 // ref: src/app/global.components/mc-show-process.component.js

 let MCShowProcessComponentController;

 var controller;
 var element;
 var scope;

 var usage = '<mc-show-process process-id="{{outside}}"></mc-show-process>'

 beforeEach(module('materialscommons'));

 // beforeEach(inject(function($rootScope, $compile){
 //     scope = $rootScope.$new();
 //     element = angular.element(usage);
 //     element = $compile(element)(scope);
 //     scope.outside = '12345'; // process_id
 //     scope.$apply();
 // }));

 beforeEach(inject(function($rootScope, $componentController){
 scope = $rootScope.$new();
 MCShowProcessComponentController =
 $componentController('mcShowProcess', {$scope: scope}, {processId: '12345'});
 }));

 // Verify our controller exists
 it('should be defined', function() {
 expect(MCShowProcessComponentController).toBeDefined();
 });
 });
 */
