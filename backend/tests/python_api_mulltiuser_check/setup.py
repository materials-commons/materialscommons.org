from setuptools import setup, find_packages

setup(
    name='python_api_mulltiuser_check',
    version='0.1.0',
    packages = find_packages(),
    install_requires = [
        'materials_commons',
    ],
)
